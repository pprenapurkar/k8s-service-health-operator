[pytest]
# Put the project root on sys.path so `import operator_app` resolves to the
# local package during tests (no cluster required for the pure-function tests).
pythonpath = .
testpaths = tests
addopts = -v
